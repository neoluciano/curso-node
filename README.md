# Notas

Este NO ES mi primer programa en NODE

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base que se va a tomar para multiplicar
                                                             [number] [required]
  -l, --listar   Lista el resultado de la tabla por consola
                                                      [boolean] [default: false]
  -h, --hasta    Limite hasta donde va a llegar la tabla. Por defecto, contara
                 hasta 10.                                [number] [default: 10]
```